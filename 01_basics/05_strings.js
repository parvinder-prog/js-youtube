const name = "palvi"
const repoCount = 10

// console.log(name + repoCount + " Value"); // it's outdated syntax
// this below syntax is used 
console.log(`Hello My name is ${name} and my repo count is ${repoCount}`);

const gameName = new String('parvinder')